import os
import sys

# Change the working directory to the directory of this script
os.chdir(sys.path[0])
import os
import sys

# Change the working directory to the directory of this script
os.chdir(sys.path[0])
